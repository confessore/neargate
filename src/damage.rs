
//#[derive(Default, Copy, Clone, Reflect, Hash)]
pub enum MagicalDamage {
    //#[default]
    Default,
    Air,
    Earth,
    Fire,
    Water
}

//#[derive(Default, Copy, Clone, Reflect, Hash)]
pub enum PhysicalDamage {
    //#[default]
    Default,
    Crushing,
    Piercing,
    Slashing
}